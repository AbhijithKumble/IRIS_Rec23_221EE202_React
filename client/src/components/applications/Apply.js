const Apply = () => {

    return (
        <>
            <span>All Application with select application option and routes will be application/name of application</span>
        </>
    );
};

export default Apply;